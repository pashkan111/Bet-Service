version: '3'

services:
  db:
    image: postgres:13
    restart: always
    env_file:
      - ./.env
    volumes:
      - ./volumes/postgresql/pg-data:/var/lib/postgresql/data
    ports:
      - "5439:5432"
